# Supabase 配置指南

本项目使用 Supabase 作为后端数据库。请按照以下步骤完成配置。

## 1. 创建项目

1. 访问 [Supabase](https://supabase.com/) 并注册/登录账号。
2. 点击 "New Project" 创建一个新项目。
3. 填写项目名称（例如 `work-log-app`），设置数据库密码，选择一个离你较近的 Region（例如 Singapore 或 Tokyo）。
4. 等待项目创建完成（通常需要几分钟）。

## 2. 创建数据表

1. 在左侧菜单栏点击 "SQL Editor"（图标类似 `>_`）。
2. 点击 "New query"。
3. 复制以下 SQL 代码并粘贴到编辑器中：

```sql
-- 你也可以直接运行项目根目录下的 schema.sql 文件
-- 下面是 schema.sql 的内容：

-- 创建工作记录表
create table public.work_records (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  start_time timestamp with time zone not null,
  end_time timestamp with time zone not null,
  total_hours numeric not null,
  total_salary numeric not null,
  note text
);

-- 启用 Row Level Security (RLS)
alter table public.work_records enable row level security;

-- 创建策略：允许所有用户查看记录
create policy "Allow public access to work_records"
on public.work_records
for all
using (true)
with check (true);

-- 创建策略：允许所有用户插入记录
create policy "Allow public insert to work_records"
on public.work_records
for insert
with check (true);

-- 创建策略：允许所有用户删除记录
create policy "Allow public delete to work_records"
on public.work_records
for delete
using (true);
```

4. 点击右下角的 "Run" 按钮执行 SQL。
5. 执行成功后，可以在 "Table Editor" 中看到 `work_records` 表。

## 3. 获取配置信息

1. 点击左侧菜单底部的 "Project Settings"（设置图标）。
2. 选择 "API"。
3. 在 `Project URL` 和 `Project API keys` 部分，找到以下信息：
    - **URL**: 你的项目 URL（例如 `https://xxxxxxxx.supabase.co`）
    - **anon public**: 你的匿名公钥（Anon Key）

## 4. 连接应用

1. 打开本项目主页。
2. 点击页面右上角的 "⚙️ Supabase 配置"。
3. 将上一步获取的 **URL** 和 **Anon Key** 填入对应的输入框。
4. 点击 "保存配置"。

现在，你就可以开始使用记账功能了！
